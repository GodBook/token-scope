# TokenScope v1.1.1 更新日志

> **发布版本**：v1.1.1  
> **发布日期**：2026 年 9 月  
> **更新类型**：体验进化 & 稳定性提升（Patch Release）  
> **适用平台**：Windows 10 / 11 (x64) 本地桌面版 & 绿色免安装版

---

## 💡 版本导语

在上一大版本 **v1.1.0** 中，TokenScope 正式带来了基于本地硬件加速的 **OCR 截图智能识别录入**，彻底改变了以往手动挨个抄写 Token 用量的繁琐流程。发布后我们收到了大量关于多控制台表格排版、分时用量累加、高分屏适配等真实使用场景的宝贵反馈。

**v1.1.1** 作为紧随其后的细节打磨与体验升级版本，聚焦于**更强的 OCR 容错识别能力**、**更从容的冲突处理机制**以及**多项视觉与交互体验优化**，让个人 AI Token 追踪记录变得更精准、更简单、更安心。

---

## 🌟 核心亮点速览

1. **OCR 算法再进化**：深度适配主流 API 聚合面板（OneAPI、NewAPI、OpenRouter、SiliconFlow 等）的复杂表格排版，智能过滤表头噪音，模型名与用量匹配更精准。
2. **冲突智能合并（累加 / 覆盖 / 跳过）**：当日记录已存在时，支持一键选择「累加用量」或「覆盖更新」，完美满足一天内多次统计与补登用量的场景。
3. **识别结果全景校对**：新增识图结果原图查看与缩放对比，并支持「一键全选 / 反选」，批量录入更快一步。
4. **图表与报表体验细化**：优化 ECharts 高分屏下的千分位 Tooltip 显示，Excel 导出无缝保留 OCR 提取的费用备注与明细。

---

## 🔍 详细更新内容

### 1. OCR 智能识别与录入增强

- **多平台控制台表格深度适配**：
  - 针对各类第三方中转站（NewAPI / OneAPI）、海外聚合平台（OpenRouter）以及官方控制台（OpenAI / Anthropic / Google AI Studio / DeepSeek）的表格行间距、虚线分隔符与对齐方式进行了聚类算法优化，大幅降低跨行错位概率。
  - 智能过滤表头关键字干扰（如自动忽略 `Model Name`、`Prompt Tokens`、`Completion Tokens`、`Total Tokens`、`Cost` 等表头文本）。
- **长模型名与特殊字符切分修复**：
  - 修复较长模型版本号（如 `claude-3-5-sonnet-20241022`、`gemini-1.5-pro-002` 等）在特定 DPI 字符间距下容易被截断为多段的问题。
  - 优化斜杠零（Slashed Zero `0̸`）、希腊字母 $\theta$ 及编程等宽字体在深色背景下的还原准确率。
- **更宽泛的数字与货币格式兼容**：
  - 增强带有空格与千分符的金额识别（如 `$ 12.50`、`¥ 88.00`），并正确识别简写单位（如 `250.5k`、`1.25M`、`3.8B`）。

---

### 2. 交互与录入流程打磨

- **智能数据冲突策略**：
  - 在保存识别条目时，若检测到目标日期已有同名模型的统计数据，系统将弹出智能操作提示：
    - ➕ **累加 (Accumulate)**：将识别值自动叠加到已有数值上（例如下午统计时追加上午的用量）。
    - 🔄 **覆盖 (Overwrite)**：用最新截图中的用量直接替换历史旧值。
    - ⏭️ **跳过 (Skip)**：跳过当前冲突条目，保持原有数据不动。
- **识图校对抽屉体验优化**：
  - 识图预览抽屉内支持**点击/双击查看截图放大图**，便于核对模糊小字。
  - 识别结果列表顶部新增**「全选 / 反选 / 仅选新增模型」**快捷按钮，多条记录勾选更加快捷。
  - 优化拖拽上传区域的悬浮交互，释放文件时增加丝滑的呼吸光晕反馈。

---

### 3. 数据可视化与导出优化

- **图表展示与 Tooltip 优化**：
  - 修复多模型对比柱状图在特定数值跨度较大时的 Label 视觉重叠问题。
  - 优化趋势图折线 Tooltip 的数值展示，自动保持千分位分隔符，大额用量一目了然。
- **Excel 导出进一步规范**：
  - 导出表格新增对 OCR 识别备注（包含提取到的调用花费）的单独格式化展示，方便月度预算报销与对账。
  - 优化 Excel 样式边框与列宽自适应算法，打开报表时视觉更加规整。

---

### 4. 性能提升与问题修复

- **内存与渲染性能**：
  - 优化高分辨率（2K/4K）大图粘贴或拖入时的图像预处理管道，降低瞬时内存与显存占用。
  - 修复多条记录快速批量保存时轻微的 UI 状态重新渲染抖动。
- **日常 Bug 修复**：
  - 修复在快速切换日期筛选器时，个别编辑框未及时失焦导致的值缓存不同步问题。
  - 修复无 Windows 原生 OCR 支持的旧系统环境下降级到 WebAssembly Tesseract 时偶发的初始化延迟。
  - 优化在断网或 GitHub Releases 网络波动时，软件内版本更新检查的错误处理，提供更友好的重试提示而不打扰正常使用。

---

## 🔒 数据安全与升级说明

1. **100% 本地优先，零数据外流**：
   - TokenScope 的所有统计数据存储于本地 SQLite 数据库（`token-statistics.sqlite`）。
   - OCR 识别完全依赖 Windows 本地原生离线 OCR 模块，识别过程不经过任何第三方服务器，不截取、不上传您的任何敏感用量与密钥信息。
2. **无损平滑升级**：
   - 使用内置更新或下载免安装覆盖更新时，应用将在启动时自动对 SQLite 数据库进行快照备份（保存在 `%APPDATA%`），历史统计记录 100% 完好无损。

---

## 📥 获取与安装

- **自动更新**：打开 TokenScope → 点击左下角 **「设置」** → 点击 **「检查更新」**，即可一键自动下载并无损完成更新。
- **手动下载**：前往 GitHub Releases 页面下载最新的 `TokenScope-v1.1.1-Setup.msi` 或绿色便携版压缩包。

> 感谢各位开发者与 AI 重度用户的支持与反馈！如果 TokenScope 帮您厘清了日常 Token 消耗，欢迎在 GitHub 上点个 ⭐️ Star 支持我们！
